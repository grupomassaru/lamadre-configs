name: Publish to GitHub Packages

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Publish @grupolamadre/configs
    runs-on: [self-hosted, linux, x64, lamadre]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@grupolamadre'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Verify package contents
        run: npm pack --dry-run

      - name: Publish
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify:
    name: Tag summary
    runs-on: [self-hosted, linux, x64, lamadre]
    needs: publish
    if: always()
    steps:
      - name: Summarize
        run: |
          echo "## Publish result: ${{ needs.publish.result }}" >> $GITHUB_STEP_SUMMARY
          echo "Tag: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "Package: @grupolamadre/configs@$(echo ${{ github.ref_name }} | sed 's/^v//')" >> $GITHUB_STEP_SUMMARY
